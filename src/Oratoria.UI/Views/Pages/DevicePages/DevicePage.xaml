<Page x:Class="Oratoria.UI.Views.Pages.DevicePages.DevicePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:vm="clr-namespace:Oratoria.UI.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="600"
      d:DesignWidth="600"
      d:DataContext="{d:DesignInstance Type=vm:DevicePageVM}"
      Title="DevicePage"
      Background="{DynamicResource Brush.Surface}">

    <Page.Resources>
        <Style x:Key="DeviceTabItem" TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource Brush.Text}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Background" Value="{DynamicResource Brush.Button.Background}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource Brush.BorderLight}"
                                BorderThickness="0.5,0.5,0.5,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.FontWeight="Bold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Brush.Surface}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DeviceTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource Brush.Surface}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource Brush.BorderLight}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0"
                                      IsItemsHost="True"
                                      Background="{DynamicResource Brush.Button.Background}"/>
                            <Border Grid.Row="1"
                                    Background="{DynamicResource Brush.Surface}"
                                    BorderBrush="{DynamicResource Brush.BorderLight}"
                                    BorderThickness="0.5,0,0,0">
                                <ContentPresenter ContentSource="SelectedContent"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DeviceTabItem}"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource Brush.Button.Background}"
                Padding="6,4">
            <TextBlock Text="{Binding DeviceName}"
                       Foreground="{DynamicResource Brush.Text}"
                       FontSize="14"
                       FontWeight="Bold"
                       Margin="10,0,0,0"/>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Статус" Style="{DynamicResource RealValueLabel}"/>
            <Label Grid.Column="1" Content="{Binding Status}" Style="{DynamicResource DynamicRealValueLabel}"/>
        </Grid>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Errors}">
            <ItemsControl.Style>
                <Style TargetType="ItemsControl">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasErrors}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ItemsControl.Style>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding}"
                           Style="{DynamicResource RealValueLabel}"
                           Foreground="{DynamicResource Brush.Warning}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TabControl Grid.Row="3" Style="{StaticResource DeviceTabControl}">
            <TabItem Header="Действия">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Actions}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0"
                                            Content="{Binding DisplayName}"
                                            Command="{Binding Command}"
                                            Style="{DynamicResource FrameButton}"
                                            FontWeight="Normal"
                                            Height="32"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left"
                                            Padding="20,0,0,0"/>
                                    <Border Grid.Column="1"
                                            BorderBrush="{DynamicResource Brush.BorderLight}"
                                            BorderThickness="0,0.5,0,0"
                                            Height="32">
                                        <ContentControl Content="{Binding ParameterControl}"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Left"
                                                        Margin="8,0,12,0"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Настройки">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Параметр" Style="{DynamicResource RealValueHeaderLabel}" Margin="15,8,0,0"/>
                        <Label Grid.Column="1" Content="Диапазон" Style="{DynamicResource RealValueHeaderLabel}" Margin="15,8,0,0"/>
                        <Label Grid.Column="2" Content="Задание" Style="{DynamicResource RealValueHeaderLabel}" Margin="15,8,0,0"/>
                    </Grid>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Settings}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Label Content="{Binding DisplayNameWithUnit}" Style="{DynamicResource RealValueLabel}"/>
                                        <Label Grid.Column="1" Content="{Binding RangeText}" Style="{DynamicResource RealValueLabel}"/>
                                        <Border Grid.Column="2"
                                                BorderBrush="{DynamicResource Brush.BorderLight}"
                                                BorderThickness="0,0.5,0,0"
                                                Height="32">
                                            <TextBox Text="{Binding ValueText, UpdateSourceTrigger=LostFocus}"
                                                     Style="{DynamicResource TextBoxInput}"
                                                     VerticalContentAlignment="Center"
                                                     FontSize="16"
                                                     Height="26"
                                                     Width="110"
                                                     HorizontalAlignment="Left"
                                                     Margin="8,0,0,0"/>
                                        </Border>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <TabItem Header="Сигналы">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Label Content="Входы" Style="{DynamicResource RealValueHeaderLabel}" Margin="15,8,0,0"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding InputSignals}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Content="{Binding DisplayName}" Style="{DynamicResource RealValueLabel}"/>
                                            <Border Grid.Column="1"
                                                    BorderBrush="{DynamicResource Brush.BorderLight}"
                                                    BorderThickness="0,0.5,0,0"
                                                    Height="32"
                                                    MinWidth="90">
                                                <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                                                    <CheckBox IsChecked="{Binding BoolValue, Mode=TwoWay}"
                                                              Visibility="{Binding BoolVisibility}"
                                                              HorizontalAlignment="Center"/>
                                                    <TextBox Width="80"
                                                             Height="26"
                                                             FontSize="16"
                                                             VerticalContentAlignment="Center"
                                                             Text="{Binding TextValue, UpdateSourceTrigger=LostFocus}"
                                                             Style="{DynamicResource TextBoxInput}"
                                                             Visibility="{Binding TextVisibility}"/>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <Border Grid.Column="1"
                            Width="1"
                            Background="{DynamicResource Brush.BorderLight}"
                            Margin="0,8,0,0"/>

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Label Content="Выходы" Style="{DynamicResource RealValueHeaderLabel}" Margin="15,8,0,0"/>
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding OutputSignals}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Content="{Binding DisplayName}" Style="{DynamicResource RealValueLabel}"/>
                                            <Border Grid.Column="1"
                                                    BorderBrush="{DynamicResource Brush.BorderLight}"
                                                    BorderThickness="0,0.5,0,0"
                                                    Height="32"
                                                    MinWidth="90">
                                                <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                                                    <CheckBox IsChecked="{Binding BoolValue, Mode=TwoWay}"
                                                              Visibility="{Binding BoolVisibility}"
                                                              HorizontalAlignment="Center"/>
                                                    <TextBox Width="80"
                                                             Height="26"
                                                             FontSize="16"
                                                             VerticalContentAlignment="Center"
                                                             Text="{Binding TextValue, UpdateSourceTrigger=LostFocus}"
                                                             Style="{DynamicResource TextBoxInput}"
                                                             Visibility="{Binding TextVisibility}"/>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </TabItem>

            <TabItem Header="Журнал">
                <DataGrid ItemsSource="{Binding DeviceLogs}"
                          Style="{StaticResource LogDataGridStyle}"
                          RowStyle="{StaticResource LogDataGridRowStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Время" Binding="{Binding Timestamp, StringFormat='{}{0:dd-MM-yyyy HH:mm:ss}'}" Width="150"/>
                        <DataGridTextColumn Header="Уровень" Binding="{Binding Level}" Width="80"/>
                        <DataGridTextColumn Header="Сообщение" Binding="{Binding Message}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
