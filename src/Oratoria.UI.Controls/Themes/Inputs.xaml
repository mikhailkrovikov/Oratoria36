<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="Input.TextBox" TargetType="TextBox">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Background" Value="{StaticResource Brush.Input.Background}"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Style.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
                <Setter Property="Background" Value="White"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Input.Toggle" TargetType="CheckBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0"
                                CornerRadius="8"
                                Width="46"
                                Height="30"
                                Margin="0,0,5,0">
                            <Grid>
                                <Rectangle x:Name="BackgroundRect"
                                           Fill="{StaticResource Brush.Toggle.Track}"
                                           Stroke="{StaticResource Brush.BorderLight}"
                                           RadiusX="7"
                                           RadiusY="7"
                                           Height="14"/>
                                <Ellipse x:Name="SwitchThumb"
                                         Width="20"
                                         Height="20"
                                         HorizontalAlignment="Left"
                                         VerticalAlignment="Center"
                                         Margin="2"
                                         Fill="White"
                                         Stroke="{StaticResource Brush.BorderLight}"
                                         StrokeThickness="1"/>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Right"/>
                            <Setter TargetName="BackgroundRect" Property="Fill" Value="{StaticResource Brush.Toggle.On}"/>
                            <Setter TargetName="SwitchThumb" Property="Fill" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter TargetName="SwitchThumb" Property="HorizontalAlignment" Value="Left"/>
                            <Setter TargetName="BackgroundRect" Property="Fill" Value="{StaticResource Brush.Toggle.Off}"/>
                            <Setter TargetName="SwitchThumb" Property="Fill" Value="{StaticResource Brush.Toggle.OffThumb}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TextBoxInput" TargetType="TextBox" BasedOn="{StaticResource Input.TextBox}"/>
    <Style x:Key="ToggleSwitchStyle" TargetType="CheckBox" BasedOn="{StaticResource Input.Toggle}"/>

</ResourceDictionary>
