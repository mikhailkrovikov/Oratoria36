<Window x:Class="Oratoria.UI.Controls.DialogWindows.UserMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="UserMessageBox"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Height="250"
        Width="400"
        Topmost="True"
        ResizeMode="NoResize"
        Background="#FFE0E0E0"
        BorderBrush="#A0A0A4 "
        BorderThickness="2">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Oratoria.UI.Controls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="5"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
        </Grid.ColumnDefinitions>
        <Rectangle x:Name="HeaderBar" Grid.RowSpan="2" Grid.ColumnSpan="3" Fill="#C0C0C0"/>

        <TextBlock
            x:Name="CaptionTB"
            TextWrapping="Wrap"
            FontSize="16"
            Grid.RowSpan="2"
            Grid.ColumnSpan="2"
            Style="{DynamicResource HeaderTextBlock}"
            VerticalAlignment="Center"
            Grid.Column="1"
            Margin="15,0,0,0"/>

        <TextBlock
            x:Name="TextTB"
            TextWrapping="Wrap"
            Grid.Row="2"
            Grid.Column="1"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            FontSize="14"/>

        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="1">
            <Button Content="Ок" FontSize="16" x:Name="OKbutton" Width="180" Style="{DynamicResource BaseEnableButton}" Click="OKbutton_Click"/>
            <Button Content="Отмена" FontSize="16" x:Name="CancelButton" Width="180" Style="{DynamicResource BaseEnableButton}" Margin="25,0,0,0" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
