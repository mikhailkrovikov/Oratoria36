<UserControl x:Class="Oratoria36.UI.UserElements.GasLineUE"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Oratoria36.UI.UserElements"
             mc:Ignorable="d" 
             Height="24" MinHeight="24" MaxHeight="24"
             d:DesignWidth="200">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>   

        <Line Grid.Column="0"
              X1="0" Y1="12" 
              X2="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid, Mode=FindAncestor}}"
              Y2="12"
              Stroke="#A0A0A4" 
              StrokeThickness="4"/>


        <StackPanel Grid.Column="1"
                    Orientation="Horizontal"
                    Height="24">

            <Path Data="M 5,0 L 0,5 L 5,10" 
                  Stroke="#A0A0A4"
                  StrokeThickness="4"
                  Stretch="Uniform"
                  Width="13"
                  Height="24"
                  Margin="-2,0,0,0"/>
            <TextBlock Text="{Binding GasName, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                       Foreground="#3F3F3F" FontWeight="Bold"
                       FontSize="18" Width="34" TextAlignment="Left" Margin="5,0,0,0"/>

        </StackPanel>
    </Grid>
</UserControl>