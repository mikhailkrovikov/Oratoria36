<Page x:Class="Oratoria36.UI.Signals.Module2SignalsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Oratoria36.UI.Signals"
      mc:Ignorable="d"
      Title="Module2SignalsPage"
      Background="#FFE0E0E0"
      Height="890" Width="1920">

    <Grid Margin="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ВХОДЫ -->
        <Border Grid.Column="0" Background="#FFF5F5F5" CornerRadius="16" Padding="20" BorderBrush="#FFAAAAAA" BorderThickness="1">
            <DockPanel>
                <Label Content="Дискретные входы" Style="{StaticResource HeaderLabel}" FontSize="20" DockPanel.Dock="Top" Margin="0,0,0,10" />

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="DigitalInputGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="0.4*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Заголовки -->
                        <Label Content="№" Grid.Row="0" Grid.Column="0" Style="{StaticResource HeaderLabel}" />
                        <Label Content="Сигнал" Grid.Row="0" Grid.Column="1" Style="{StaticResource HeaderLabel}" />
                        <Label Content="Значение" Grid.Row="0" Grid.Column="2" Style="{StaticResource HeaderLabel}" />
                    </Grid>
                </ScrollViewer>
            </DockPanel>
        </Border>

        <!-- ВЫХОДЫ -->
        <Border Grid.Column="2" Background="#FFF5F5F5" CornerRadius="16" Padding="20" BorderBrush="#FFAAAAAA" BorderThickness="1">
            <DockPanel>
                <Label Content="Дискретные выходы" Style="{StaticResource HeaderLabel}" FontSize="20" DockPanel.Dock="Top" Margin="0,0,0,10" />

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="OutputDataGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.2*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="0.4*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Заголовки -->
                        <Label Content="№" Grid.Row="0" Grid.Column="0" Style="{StaticResource HeaderLabel}" />
                        <Label Content="Сигнал" Grid.Row="0" Grid.Column="1" Style="{StaticResource HeaderLabel}" />
                        <Label Content="Управление" Grid.Row="0" Grid.Column="2" Style="{StaticResource HeaderLabel}" />
                    </Grid>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</Page>
